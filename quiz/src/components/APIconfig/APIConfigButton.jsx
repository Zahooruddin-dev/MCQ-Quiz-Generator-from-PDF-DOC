import React from 'react';

const APIConfigButton = ({ onSubmit }) => {
  return (
    <button type="submit" className="btn">
      Start Using AI Quiz Generator
    </button>
  );
};

export default APIConfigButton;